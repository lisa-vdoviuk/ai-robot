# Core web server
Flask>=3.0.0
Flask-Sock>=0.7.0
simple-websocket>=1.0.0

# Config / utils
PyYAML>=6.0.1
requests>=2.31.0
tqdm>=4.66.0
numpy>=1.24

# SSL for LAN HTTPS microphone access
pyOpenSSL>=24.0.0

# STT
vosk>=0.3.45

# Optional local LLM support; Groq works with requests only.
# llama-cpp-python>=0.3.8
huggingface_hub>=0.23.0

# Offline TTS: Kokoro ONNX. No PyTorch required.
kokoro-onnx>=0.5.0
soundfile>=0.12.1

# Vision: YOLO ONNX. Install python3-opencv via apt on Raspberry Pi.
onnxruntime>=1.17.0
