Model files are not included in the zip.

Run:

```bash
python scripts/download_models.py
```

Default v2 model paths:

- LLM: `models/llm/qwen2.5-3b-instruct-q4_k_m.gguf`
- STT: `models/vosk/vosk-model-en-us-0.22-lgraph`
- TTS: `models/piper/en_US-ryan-high.onnx` plus `.onnx.json`
